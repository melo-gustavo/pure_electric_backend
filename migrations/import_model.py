# Write all models for mapping to the database here, so that Alembic can detect them and generate migrations.

from models.order import Order
from models.product import Product
from models.user import User